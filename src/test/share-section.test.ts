import { describe, it, expect } from "vitest";

describe("ShareSection (Deprecated)", () => {
  it("is no longer used - replaced by DownloadSection", () => {
    // This component has been replaced with DownloadSection
    // Tests for Download functionality are in download-section.test.ts
    expect(true).toBe(true);
  });
});

